## Spring Boot Ratelimit with Leaky Bucket

### Branches
`master`: Simple implementation of rate limiting <br>
`bucket-per-ip-with-caching`: Same as on `master` but with IP based buckets and Spring Caching
